meta {
  name: Get media token
  type: http
  seq: 9
}

post {
  url: {{baseUrl}}/api/v1/rooms/{{roomId}}/media/token
  body: none
  auth: none
}

headers {
  Cookie: reso_owner_session={{ownerSessionToken}}
}
