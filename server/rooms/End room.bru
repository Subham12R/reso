meta {
  name: End room
  type: http
  seq: 7
}

post {
  url: {{baseUrl}}/api/v1/rooms/{{roomId}}/end
  body: none
  auth: none
}

headers {
  Cookie: ruse_owner_session={{ownerSessionToken}}
}

vars:pre-request {
  baseUrl: localhost:8080
}
